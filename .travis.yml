language: elixir
elixir:
  - '1.10'
otp_release: '23.0'

script:
  - mix format --check-formatted
  - mix dialyzer
  - mix coveralls.travis

cache:
  directories:
    - priv/plts

# jobs:
#   include:
#     - elixir: '1.9'
#       otp_release: '20.0'
